import 'package:flutter/material.dart';

class ColorPalitra {
    static const colorPalitra = [ red, blue, green, white, grey, green, teal ];
    static const red = Color(0xFF757190);
    static const blue = Color(0xFF24445f);
    static const green = Color(0xFF70acaa);
    static const white = Colors.white;
    static const grey = Colors.grey;
    static const teal = Colors.teal;
}